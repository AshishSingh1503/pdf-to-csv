// Legacy shim kept for compatibility; the toast system has been removed.
// DELETED: re-export shim for useToast - kept temporarily for compatibility
export { default, useToast } from './useToast.jsx'
